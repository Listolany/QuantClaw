# 待解决问题

## 问题标题
OpenClaw 在主会话中出现“技能边界与全局记忆规则冲突”，导致 `astock-data` 场景被自动推进到策略生成/回测提交流程。

## 背景说明
- 用户在 OpenClaw 控制台会话中提出 A 股策略需求后，助手先读取 `astock-data/SKILL.md`，随后输出“参数确认 + 回复‘开始生成’触发下一轮”。
- 用户回复“开始生成”后，助手继续执行：写策略文件、`py_compile`、调用 `pipeline_orchestrator.py submit`。
- 用户质疑是否“切错 skill”，后端核实显示问题更本质是：规则冲突与缺失目标技能，而非单一模板词触发。

## 已核实事实（证据）
- 会话映射：`/root/.openclaw/agents/main/sessions/sessions.json`
  - `agent:main:main -> 25fba122-1894-4632-87f3-dc52f73742b1.jsonl`
- 真实会话日志：`/root/.openclaw/agents/main/sessions/25fba122-1894-4632-87f3-dc52f73742b1.jsonl`
  - 先读取 `~/.openclaw/workspace/skills/astock-data/SKILL.md`
  - 再按“三轮协议”引导用户回复“开始生成”
  - 然后执行写策略与编排器提交命令
- 全局记忆规则：`/root/.openclaw/workspace/MEMORY.md`
  - 明确要求：策略/回测类请求使用“三轮交互协议”，并以“开始生成”作为第 2 轮触发词
- 技能边界规则：`/root/.openclaw/workspace/skills/astock-data/SKILL.md`
  - 明确要求：本技能仅用于数据查询；若涉及回测/策略执行必须切换到 `quant-strategy-assistant`
- 目标技能运行时未加载：`quant-strategy-assistant` 文件存在于 workspace 但以软链接部署，被 OpenClaw 路径安全检查拦截，未出现在 `sessions.json` 的 `resolvedSkills` 中（详见问题2）

## 核心根因（结论）
1. `MEMORY.md` 的主会话硬规则（策略三轮协议）与 `astock-data` 的“仅数据查询”边界冲突。
2. `astock-data` 指定的切换目标 `quant-strategy-assistant` 文件存在但以软链接部署，被 OpenClaw 安全机制拦截未加载到运行时技能列表，导致边界要求无法落地（已通过实体复制修复，详见问题2）。
3. 结果表现为：流程可运行，但角色边界不一致，用户会感知为“行为漂移/不可预测”。

## 影响范围
- 用户预期一致性：中高风险（“只想查询”用户会被推进到执行链路）。
- 可解释性：中风险（表面是 `astock-data`，行为却进入策略编排）。
- 稳定性：中风险（提交链路已出现参数/策略类识别错误案例）。

## 复现步骤
1. 在主会话提出“策略/回测/均线筛选”需求。
2. 助手读取 `astock-data` 技能后，输出“参数确认 + 请回复开始生成”。
3. 用户发送“开始生成”。
4. 助手进入代码生成、编译、编排器提交流程。

## 修复目标
- 明确“数据查询模式”与“策略执行模式”的控制边界，避免在同一技能语义下隐式跳转到执行链路。

## 修复建议（按优先级）
### P0（必须）
- 统一规则源：在主会话执行前增加规则冲突检测，若技能边界与 MEMORY 冲突，直接报配置错误并停止自动推进。
- 补齐或替换目标技能：新增 `quant-strategy-assistant`，或把 `astock-data` 的切换目标改为当前存在的可执行技能。

### P1（建议）
- 增加意图闸门：对“开始生成”触发增加前置条件（已确认策略模式 + 已确认执行意图 + 已确认参数完整）。
- 增加审计字段：会话日志记录 `selected_skill`、`rule_source`、`mode_transition_reason`，便于定位问题。

### P2（优化）
- 在 UI 侧显式展示当前模式（查询/生成/提交）与触发来源（MEMORY/skill/system），降低用户心智负担。

## 验收标准
- 当仅启用 `astock-data` 且无有效策略技能时，策略请求应停留在“查询能力说明/缺失能力提示”，不得进入生成提交。
- 当策略技能存在且用户明确执行时，才允许“开始生成”进入第 2 轮。
- 会话日志中可追踪每次模式切换的依据与来源。

## 备注
- 本文档用于后续修复追踪与交接，避免缺失上下文时重复排查。

---

## 问题2：软链接方式安装 Skill 被 OpenClaw 安全机制拒绝

### 背景
之前设计为从开源包软链接到 workspace（`ln -s /opt/quant-claw-push/skills/xxx ~/.openclaw/workspace/skills/xxx/SKILL.md`），目的是保持开源包为唯一编辑源。

### 根因
OpenClaw 技能发现流程中 `resolveContainedSkillPath()` 会对候选文件做 `realpath()` 解析，再用 `isPathInside()` 校验真实路径是否在 workspace 根目录内。软链接指向 workspace 外部（如 `/opt/quant-claw-push/`）会被判定为路径逃逸，直接跳过该技能。

### 证据
- 源码位置：`/usr/lib/node_modules/openclaw/dist/skills-DBpct2ZG.js` 第 1152-1162 行
- 实测：`quant-strategy-assistant` 软链接时 `inside=False`，替换为实体复制后 `inside=True`，`openclaw skills list` 立即显示 `✓ ready`

### 已修复
- 将软链接替换为 `cp -r` 实体复制
- README 中已补充安装说明，明确标注"不要用软链接"
- 更新 Skill 时重新 `cp -r` 覆盖即可

### 状态：已解决

---

## 问题3：astock-data 与 quant-strategy-assistant 双 Skill 场景说明缺失

### 背景
- `astock-data`：面向主观投资者，提供数据查询（K线、行情、成分股等）
- `quant-strategy-assistant`：面向量化用户，自然语言→策略生成→回测→优化

两者底层都依赖 qgdata 数据源，但定位不同。

### 待解决
- [ ] `astock-data` 目前不在开源包中，是否也需要放入开源包？还是保持 clawhub 独立分发？
- [ ] 两个 Skill 的适用场景说明需在 README 或独立文档中清晰标注（哪些需求走哪个 Skill）
- [ ] `astock-data/SKILL.md` 中"必须切换到 quant-strategy-assistant"这条边界声明是否还保留？当 quant-strategy-assistant 已正常加载后，agent 能否正确执行切换？需实测验证
- [x] `MEMORY.md` 中的策略三轮协议已从全局规则移到路由指令（2026-03-09 完成，MEMORY.md 仅保留一行路由+"全局配置"+"历史Bug"，策略细节全部由 SKILL.md 承载）

### 状态：部分解决（MEMORY.md 已修；其余 3 项待讨论）

---

## 问题4：开源包跨 Agent / 跨平台兼容

### 背景
开源包原始 SKILL.md 中包含 OpenClaw 专属语义和 Linux-only 命令，导致非 OpenClaw Agent 和 Windows 用户无法直接使用。

### 已完成的修改
1. **SKILL.md 第0步**：Linux-only `find` 命令替换为跨平台 Python `pathlib.rglob()` 发现逻辑
2. **SKILL.md YAML 头**：保留 `metadata.openclaw` 同时增加通用 `metadata.requires`，并添加兼容说明
3. **SKILL.md 禁止清单**：`workspace` 专属表述改为通用表述（"项目目录"/"历史报告文件"）
4. **SKILL.md 环境变量**：`OPENCLAW_CONTROL_URL` 标注为 OpenClaw 环境专用，非必需
5. **README**：新增"非 OpenClaw Agent 使用方式"小节

### 已验证
- 修改后的 SKILL.md 在 OpenClaw 中加载正常（`openclaw skills list` → `✓ ready`）
- 开源包与 workspace 文件 diff 一致

### 无法完全兼容的点
- Agent 技能自动发现机制：各平台私有协议，无法统一
- 会话持久化体验：依赖各 Agent 的上下文能力，功能上靠 run_id 兼容，体验无法保证一致
- Agent 权限模型：禁止本地命令执行的环境无法跑全自动链路

### 状态：已完成基础兼容改造

---

## 问题5：LLM 生成策略代码的高频 Bug 未被静态检查拦截

### 背景
`pipeline_orchestrator.py` 的 `_lint_strategy()` 在 submit 前做正则静态检查，但规则集只覆盖了少量已知坑（`am.ma()`、错误 import 路径等），大量 LLM 高频错误直接进入回测运行时才崩溃，用户在监控页看到"执行失败"。

### 根因
通过分析全部 75+ 历史 run（39 个代码类失败），归纳出以下高频 bug 类型未被 lint 拦截：

| 错误类型 | 历史出现次数 | 根因 |
|---|---|---|
| `__init__` 签名参数不足 | **6次** | LLM 用 `(self, context)` 代替 vnpy 要求的 5 参数构造函数 |
| `am.sma(n, 1)` 第二参数误传整数 | **5次** | LLM 以为第二参数是"前一根K线偏移"，实际是 `array:bool`，`1`=True 返回 numpy 数组 |
| numpy 数组+`and`/`or` 布尔崩溃 | **3次** | array=True 返回值直接参与 Python `and`/`or`，触发 `ValueError` |
| `bar.close` 应为 `bar.close_price` | **1次** | vnpy BarData 属性名带 `_price` 后缀 |
| 缺少 `on_init` 抽象方法 | **2次** | CTA 策略必须实现 `on_init`，否则无法实例化 |
| 裸引用 `vt_symbols` | **3次** | 方法体内用 `vt_symbols` 而非 `self.vt_symbols`，NameError |
| `self.get_bars()` 不存在 | **3次** | 应为 `self.load_bar()`/`self.load_bars()` |

### 已修复（2026-03-09）
在 `_lint_strategy()` 新增 7 条规则：

1. **`am.sma(n, integer)` 检测**（blocker/warning）：第二参数为整数时阻断；若有 `float()/[-N]` 标量提取则降级为 warning
2. **`bar.close/open/high/low` 检测**（blocker）：应为 `bar.close_price` 等
3. **`__init__` 参数数量检测**（blocker）：不足 5 个参数时阻断（不依赖是否继承 CtaTemplate）
4. **缺少 `on_init` 检测**（blocker）：CTA/Portfolio 策略必须实现
5. **裸 `vt_symbols` 检测**（blocker）：排除 `__init__` 体和 `super().__init__` 调用
6. **`self.get_bars()` 检测**（blocker）：该方法不存在
7. **`array=True` 变量+`and`/`or` 检测**（blocker）：排除 `is None`/`len()`/索引等安全用法

### 验证结果
- 历史失败 run 拦截率：22/39（56%），从原来 ~26% 提升
- 成功 run 误杀：0（新规则无一误杀；1 个来自已有旧规则）
- 剩余 17 个未拦截的失败属于：CLI 参数错误、数据引擎问题、极低频运行时逻辑 bug（日期格式、MACD 解包等），正则无法可靠捕获

### 状态：已完成（lint 层；运行时兜底见问题6）

---

## 问题6：策略代码运行时 Bug 需冒烟测试（Dry Run）兜底

### 背景
静态 lint 能拦截结构性错误（56%拦截率），但无法覆盖所有运行时 bug（如动态属性访问、数据依赖的分支逻辑）。用户提交后等数分钟才在监控页看到"执行失败"，体验差。

### 方案：采样冒烟测试（三层防御第二层）

**三层防御体系**：
1. **静态 lint**（毫秒级）：正则检查高频结构性 bug
2. **采样 dry run**（秒级）：取 ≤5 只标的 × 50 根 K线实际回放，快速暴露运行时错误
3. **完整回测**：前两层通过后才启动

**实现细节**：
- `backtest_runner.py` 新增 `--max-bars N` 参数，>0 时截断引擎数据仅回放前 N 根 K线，完成后直接 exit 0，跳过报告生成
- `pipeline_orchestrator.py` 的 `cmd_worker` 中，lint 通过后、正式回测前，用子进程跑 dry run（`--max-bars 50`，symbols 采样 ≤5 只）
- 超时上限 120 秒，失败时生成 `error_type=dryrun_error` 结构化错误，agent 可自动修复重提交
- `SKILL.md` 错误分流表已新增 `dryrun_error` 类型，处理逻辑同 `compile_error`

**成本分析**：
- 数据已缓存时：~2-3 秒（SQLite 读取 + 50 根 bar 回放）
- 首次需下载数据时：取决于采样标的数（5 只 ≈ 5 秒，远低于全市场 600+ 秒）
- 全市场策略不需要全部下载：采样 5 只即可检测 >95% 的代码 bug（因代码 bug 通常与标的无关）

### 验证结果（2026-03-09）

| 测试 | run_id | 模式 | 原始状态 | dry run 结果 | 符合预期 |
|------|--------|------|---------|-------------|---------|
| 成功CTA | 20260309_062343 | cta | completed | exit 0 | ✅ |
| 失败CTA | 20260309_054405 | cta | failed(ValueError) | exit 1 拦截 | ✅ |
| 成功portfolio | 20260308_164103 | portfolio | completed | exit 0 | ✅ |
| 失败portfolio(数据) | 20260308_120513 | portfolio | failed(BJ Exchange) | exit 0 | ✅ 数据问题非代码bug |

### 修改文件
- `backtest_runner.py`：+`--max-bars` 参数，`run_backtest()` 数据截断，dry run 模式跳过报告
- `pipeline_orchestrator.py`：`cmd_worker()` lint 后插入 dry run 子进程调用
- `SKILL.md`：错误分流表新增 `dryrun_error`

### 状态：已完成

---

## 问题7：SSE 断开时图表不实时渲染 + 缺少结果校验层

### 背景
1. 服务器在日本、用户在中国，SSE 跨境连接几乎必断。SSE 重连时 `init_axis` 清空图表再重绘，导致闪烁；poll 只做全量替换不做增量追加，每日收益柱状图不随 poll 同步渲染
2. 策略逻辑 bug（如只买不卖、direction.name=="BUY"）通过了 lint + dry run + 完整回测，缺少最终结果校验层

### 方案：Poll 主渲染 + 结果校验嵌入进度流

**改动一：Poll 主渲染通道**（`monitor_server.py` 客户端 JS）
- SSE `init_axis`：去掉清空图表代码，只更新元数据
- SSE `point`：增加日期去重（防重连 replay 重复 push），同步追加日收益柱
- poll `_pollState`：改为增量追加（只 append 新 nav 点 + 同步计算日收益追加），不全量替换
- poll 间隔：SSE 断开时 3s、SSE 正常时 15s
- SSE `final_chart`：保留全量替换（回测完成后精确数据）

**改动二：结果校验嵌入进度流**（`pipeline_orchestrator.py` + `monitor_server.py`）
- `_validate_result()` 函数：检测零交易、单边信号（有买无卖/有卖无买）、胜率极端值（100%/0%）
- 嵌入为 Step5（结果校验）→ Step6（报告生成），校验在进度约90%时推送，用户在100%前就看到告警
- `/api/result_warnings` POST 端点 + SSE 事件 + 客户端黄色告警卡片
- `SKILL.md` 第三轮分流新增 `result_warnings` 处理

### 修改文件
- `monitor_server.py`：SSE/poll 改为增量追加 + 日收益同步 + result_warnings 端点/事件/卡片
- `pipeline_orchestrator.py`：`_validate_result()` + cmd_worker Step5/Step6 重构
- `SKILL.md`：第三轮分流新增 3.5 结果校验告警处理

### 验证结果
- `_validate_result` 单元测试：5 个 case 全部通过（只买无卖、零交易、100%胜率、0%胜率、正常策略）
- 历史 run 校验：正常策略 0 误报，异常策略全部命中
- HTML 10 项检查全部通过（warn-card、去重、增量追加、日收益同步、间隔调整等）
- 编译检查通过

### 状态：已完成

---

## 问题8：Portfolio + 共享Token 频率限制提前告知

### 背景
1. 共享试用 Token 有每日 API 调用频率限制，大约支撑每天 1 次 CTA 回测
2. Portfolio 策略（多标的组合/轮动/全市场选股）数据调用量远大于 CTA（N 标的 × M 数据接口），极易触发频率限制导致回测中途失败
3. 之前的引流方案过于营销化（"解锁更多数据范围"），实际数据范围差异不大，不诚实

### 方案：预检提示 + submit 限额文案 + footer 链接
仅在**同时满足两个条件**时触发 `token_hint`：
- 需求被判定为 Portfolio 策略（关键词匹配，复用 SKILL.md 的 portfolio 判定规则）
- 当前未传 Token（会回退共享试用 Token）或正在使用共享试用 Token（`QGDATA_TOKEN` 等于 `QGDATA_SHARED_TOKEN`）

触发后输出 `token_hint` 字段，SKILL.md 指导 agent 在第 1 轮确认时**如实呈现**（不包装为广告），用户知情后可选择继续或先获取个人 Token。

当用户首轮回测成功后当日再次 submit 且命中共享 Token 频率上限时，submit 返回统一 `next_action`：
`今日免费额度已用完（1次/天）。升级数据套餐可解除限制：https://quantgo.ai/data`

监控页 footer 增加 `Powered by QuantGo` 且可点击跳转数据页。

### 修改文件
- `data_capability_guard.py`：新增 `_looks_portfolio()`、`_token_context()`、`CapabilityResult.token_hint` 字段
- `pipeline_orchestrator.py`：`submit` 在共享 token 命中频率上限时返回统一 `next_action` 文案
- `monitor_server.py`：footer 增加 `Powered by QuantGo` 可点击链接
- `SKILL.md`：第 1 轮确认步骤新增 `token_hint` 呈现指令

### 验证结果
- Portfolio+未传Token：✅ 输出 `token_hint`
- Portfolio+共享Token：✅ 输出 `token_hint`
- CTA+共享Token：✅ 不输出 `token_hint`
- Portfolio+个人Token：✅ 不输出 `token_hint`
- Portfolio 判定关键词覆盖：全市场/等权/轮动/成分股选股调仓/板块筛选前N名 均命中，单标的趋势/均线排列 不误判

### 状态：已完成

---

## 问题9：submit 异步返回导致 LLM 无法在 Round 2 内修复预检错误

### 背景
1. `cmd_submit` 启动 worker 子进程后**立即**返回 `status: "accepted"` + `monitor_url`
2. lint/dryrun 在 worker 中异步执行，submit 永远返回 accepted
3. SKILL.md 声称"submit 编译/lint/dry run 快速失败，输出含 lint_error"——实际不成立
4. LLM 收到 accepted → 告知用户"已提交"→ 用户打开监控页看到"执行失败"
5. LLM 无法在同一 turn 内修复预检错误

### 方案：submit 同步等待预检完成再返回
`cmd_submit` 启动 worker 后，轮询 monitor `/api/state` 等待预检结果（compile→lint→dryrun，最长 180s）：
- **预检通过**（step 3 success 或 step 4 running）→ 返回 `status: "accepted"` + `monitor_url`
- **预检失败**（error 出现）→ kill monitor + 返回结构化错误 JSON（`status: "lint_error"/"compile_error"/"dryrun_error"` + `error` + `strategy_file` + `next_action`），**不返回 monitor_url**
- **超时** → kill monitor + worker + 返回 `timeout_error`

LLM 侧流程变为：
1. 写策略文件 → submit（阻塞等预检）
2. 若返回错误 → 先告知用户"检测到 xxx 错误，正在修复..."→ 改代码 → 重新 submit
3. 最多 **6 轮**修复重试
4. 预检全过 → 返回 `accepted` + `monitor_url` → 告知用户

### 修改文件
- `pipeline_orchestrator.py`：`cmd_submit` 新增预检轮询、失败时 kill monitor 并返回错误 JSON
- `SKILL.md`：Round 2 更新为同步预检行为 + 修复重试改为 6 轮 + 中间反馈指令

### 验证结果
- lint 错误策略（`vnpy.trading.*`）→ submit 返回 `lint_error`，exit=1，无 monitor_url，耗时 2.5s ✅
- 正常策略（双均线 CTA）→ submit 返回 `accepted` + `monitor_url`，exit=0，耗时 6s ✅
- SKILL.md 已同步到 workspace（哈希一致）✅

### 状态：已完成

---

## 问题10：optimize 与 submit 回测链路语义不一致（账户模型未复用）

### 背景
用户在同一策略上执行：
1) `submit` 正常回测（走 `backtest_runner.py` 增强链路）  
2) `optimize` 参数优化（走 `pipeline_orchestrator.py cmd_optimize` 原生 vnpy 链路）

随后出现现象：
- 策略在 submit 可用 `self.available_cash`（增强链路注入）
- 同策略在 optimize 报错 `self.available_cash` 不存在
- 为兼容 optimize 被迫把策略改回固定手数，导致优化结果与正式回测语义偏离

### 已核实事实（证据）
- `backtest_runner.py` 的 `run_backtest()` 会注入增强能力：
  - `_patch_account_model()`（注入 `self.available_cash`/`self.total_value`/`self.closable_pos` 等）
  - `_patch_lot_compliance()`（交易所手数规则）
  - `_patch_order_guards()`（停牌/涨跌停守卫）
  - `_guard_strategy_runtime()`（运行时兼容兜底）
- `pipeline_orchestrator.py` 的 `cmd_optimize()` 直接使用原生 `vnpy_ctastrategy.backtesting.BacktestingEngine` + `run_optimization()`，未调用 `backtest_runner.run_backtest()`，因此不包含上述增强注入。
- `cmd_optimize()` 目前仅取 `args.symbols.split(",")[0]`，本质是 CTA 单标的优化链路。

### 核心根因（结论）
优化与正式回测使用了两套不同执行语义：
- submit：增强账户模型语义
- optimize：原生 vnpy 语义

导致同一策略在两条链路中可用字段、下单约束、成交行为不一致，优化结果与正式回测不可直接对比。

### 影响范围
- 一致性风险：高（优化结论可能无法复现到 submit 正式回测）
- 可解释性风险：高（用户感知为“同一策略一会儿能跑一会儿报错”）
- 策略质量风险：中高（参数搜索目标函数建立在不同交易语义上）

### 中期改造方案（推荐，待实施）
将 optimize 重构为复用 `backtest_runner` 同一账户模型语义：

1. 参数枚举仍在 orchestrator 层完成（保留现有 `optimize-params` 输入格式）
2. 每组参数不再直接调用原生 `run_optimization()`，改为调用统一回测执行函数（复用 `run_backtest()` 增强链路）
3. 每次执行返回标准统计字段（`total_return`/`sharpe_ratio`/`max_ddpercent` 等），由 orchestrator 汇总排序
4. 输出结构保持兼容（`status/completed/target_metric/results/best`），不破坏前端与 skill 协议

### 设计要求（实施时必须满足）
- 语义一致：optimize 与 submit 使用同一账户/下单守卫/手数规则
- 可复现：`best params` 在 submit 下复跑时指标偏差可解释（容忍微小数值误差）
- 性能可控：支持 `algorithm=bf/ga` 两种搜索；允许并行度配置但默认保守
- 向后兼容：CLI 入参和输出 JSON 字段尽量不变

### 验收标准
- 同一策略在 optimize 与 submit 均可使用 `self.available_cash`，不再出现“字段存在性冲突”
- 取 optimize 的 best 参数重新 submit，关键指标方向一致（收益/回撤/夏普排序不反转）
- 旧有 optimize 命令不需要改调用方式即可运行

### 当前状态
待实施（已记录，后续按本条执行）

---

## 问题11：qgdata_docs-new 更新后接口差异未同步到 skills 与依赖

### 背景
`qgdata_docs-new` 已更新，但当前 `skills` 文档、调用示例与相关依赖尚未完成新增接口同步，存在能力描述与实际 SDK/API 不一致风险。

### 待解决
- [ ] 对比新旧文档接口清单，产出新增/变更/废弃接口列表（含参数、返回、错误码差异）
- [ ] 按差异清单更新相关 `skills`（尤其 `quant-strategy-assistant/SKILL.md`）中的能力声明、调用约束与示例
- [ ] 同步检查并更新相关依赖（SDK 版本、封装层适配、调用参数映射），避免文档已更但运行时不可用
- [ ] 对受影响流程做最小回归验证（submit/optimize/数据查询主链路），确保原有功能不受影响
- [ ] 在 README 对应位置补充“新增接口支持范围、使用方式、兼容说明”

### 验收标准
- 能明确给出 `qgdata_docs-new` 相比旧版本的接口差异清单
- `skills` 与依赖层对新增接口可用且说明一致
- 主链路回归通过，未引入已有功能退化
- README 已同步更新

### 状态：待实施

---

## 7. Windows 环境下 OpenClaw 对接 QMT 模拟实盘验证

### 背景
`trade_runner.py` 已在 Linux mock 模式下全链路验证通过（策略加载→BarGenerator注入→load_bar预热→探针单mock→策略启动→优雅停止）。但真实 QMT 通信链路（`XtGateway` 连接、tick 订阅、探针单下单/撤单、策略实时交易）仅能在 Windows + QMT 环境下验证。

### 待验证项
- [ ] Windows 环境 `pip install` 安装 `vnpy_qmt` 依赖链完整性（xtquant 等）
- [ ] `XtGateway.connect()` 能正常连接已打开的 QMT 客户端
- [ ] 合约查询、tick 订阅、`symbol_limit_map` 跌停价获取正常
- [ ] 探针单完整流程：下单→QMT委托列表可见→自动撤单→确认撤销
- [ ] `LiveAccountBridge` 实时现金/持仓/市值更新（`EVENT_TICK`/`EVENT_TRADE`/`EVENT_ACCOUNT` 回调）
- [ ] `BarGenerator` 注入后日线策略在盘中能正确聚合 tick→1min→daily bar
- [ ] `_stop_flag` 文件信号在 Windows 下能触发优雅退出（因 `SIGTERM` 在 Windows 下是硬杀）
- [ ] OpenClaw Agent（在 Windows 本地运行）能通过 SKILL.md 指引正确调用 `qmt-check` → `trade` → `trade-stop` 全流程

### 状态：待实施（需 Windows + QMT 环境）

---

## 8. MiniMax Agent 使用 skill 全链路验证

### 背景
当前 `quant-strategy-assistant` SKILL.md 已在 OpenClaw（Claude 驱动）上验证过策略生成→回测→监控全链路。但 SKILL.md 的指令遵从度、JSON 解析能力、多轮修复逻辑对不同 LLM 底座有差异。MiniMax 作为潜在接入的 Agent 底座，需独立验证。

### 待验证项
- [ ] MiniMax Agent 能正确解析 SKILL.md 中的触发词路由（策略生成 vs 数据查询 vs 模拟交易）
- [ ] 策略代码生成质量：类名、import 路径、`on_bar` 签名、仓位管理逻辑是否符合 SKILL.md 约束
- [ ] `submit` 返回的 JSON（status/run_id/monitor_url）能否被正确解析并展示给用户
- [ ] 预检失败时的自动修复循环（最多6轮）是否正常触发和终止
- [ ] 结果校验告警（单边信号/零交易等）能否被 LLM 正确理解和处理
- [ ] 参数优化流程的交互（确认优化方案→执行→展示 Top N）是否流畅
- [ ] 模拟交易流程指引（`qmt-check` → `trade`）在 MiniMax 下的指令遵从度

### 状态：待实施
